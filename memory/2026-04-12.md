## 2026-04-12
- Correction lesson: active summaries and boards had drifted by still presenting Proton and Microsoft Graph / tenant-id / client-id as current work, even after the Exchange route was clarified as local Exchange SE on-prem. Fixed by moving those tracks to parked/legacy in KANBAN/STATUS, removing Graph/Proton from active status boards, and recording a stronger rule: parked tracks must not be reported as active, and local Exchange should default to EWS/Autodiscover/PowerShell unless explicit hybrid/M365 evidence exists.
- Christian explicitly wants agreements not just acknowledged but propagated structurally, thoroughly, and accurately across the real working system. From now on, when an agreement changes direction or rules, treat the work as incomplete until the relevant source-of-truth files, trackers, boards, prompts, and recurring workflows are aligned as well.
- Christian also wants stale or no-longer-relevant tracks to be closed or parked proactively, not left hanging until manually called out.
- Background build: Proton manual-finish observability now carries explicit evidence metadata (`verification_source`, `verification_checked_at`, `verification_age_seconds`) from `proton-verification-status.py`, and that freshness/source now surfaces in `proton-manual-finish-summary.py`, `protonboard.py`, `automation-board.py`, `command-board.py`, `statusboard.py`, and `clawdy-brief.py`. Verified with `py_compile`, live `proton-manual-finish-summary.py`, `protonboard.py`, and `command-board.py | grep 'proton handoff'`, which now show `source=finish-with-code` plus the artifact age instead of only a generic manual-boundary flag.
