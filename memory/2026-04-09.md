## 2026-04-09
- Heartbeat build: `web-automation-sites.py` now emits per-adapter site-health summaries (`adapter_summaries` with configured-vs-unmanaged stale counts and latest slug/age), and `automation-board.py` plus `command-board.py` surface that compact adapter-health line. Verified live: the boards now immediately show that the real managed site issue is Slack (`slack:1 stale=1 cfg=1`) while Bitwarden/example are just unmanaged stale probes.
- Follow-up heartbeat polish: those adapter-health lines now also point straight at the first slug needing attention (`slack:1 stale=1 cfg=1 -> slack-signin`, etc.) in `web-automation-sites.py`, `automation-board.py`, and `command-board.py`, so the observability now tells you not just which adapter is stale but exactly which target to refresh.
- Follow-up heartbeat polish: adapter summaries now also carry the concrete refresh command for that first attention target, and `automation-board.py` / `command-board.py` surface a `site adapter refresh:` line (currently Slack -> `python3 scripts/web-automation-dispatch.py probe-page https://app.slack.com/signin --session slack-shared --slug slack-signin`).
- Follow-up heartbeat cleanup: when that adapter-level refresh command is identical to the configured `site refresh` command, the boards now suppress the duplicate line. Result: Slack still shows clearly as the next managed stale target, but the board is less repetitive.
- Background build: `web-automation-dispatch.py desktop-probe --slug <site>` is now registry-aware, so it can infer the configured URL from `state/web-automation-sites.json` instead of requiring a duplicate `--url`; live-verified with `--slug slack-signin`, which created a fresh `out-desktop-slack-signin` probe and `desktop-status --slug slack-signin` now reports healthy/fresh Slack desktop fallback artifacts.
- Heartbeat fix: `mail-triage.py` accepteert nu ook `--recent` als alias van `--all`, en `mail-dispatch.py triage` documenteert die recent-scope nu ook expliciet; live geverifieerd met `--recent --high-only`.
- Heartbeat fix: `mail-focus.py` gebruikt nu een ruimer burst-venster (tot 50 recente mails i.p.v. 10) voor verwante-mailtelling, zodat alertgolven eerlijker worden samengevat; live geverifieerd met `--search-limit 50` en huidige Bitwarden focus blijft 7x verwant.
- Background heartbeat run: `web-automation-autopilot.py --json` refreshed the stale configured `slack-signin` probe successfully, so the managed generic site observability set is healthy again; only the unmanaged `example-home` demo probe remains stale.
- Follow-up heartbeat run repeated the same safe maintenance path successfully after `slack-signin` aged out again, confirming the autopilot refresh route is stable in practice: managed web automation sites returned to `configured_healthy=true` / `operationally_healthy=true`, with only the unmanaged `example-home` demo probe left stale.
- Background build: `command-board.py` is now more robust and faster by reusing security/task-audit data already present in `statusboard` output instead of rerunning those expensive checks, and by degrading gracefully per section instead of exiting on one slow subcommand.
- Verification: `python3 scripts/command-board.py` now completes successfully again with full status, security, task-audit, automation, mail, Proton, and tools data and no degraded sections.
- Proton signup observability now clearly surfaces post-verification completion state in the main boards: `automation-board.py` and `command-board.py` include `account_created` plus `recovery_kit_ready`, so the top-level status now shows that the flow appears to have reached the Recovery Kit/account-created stage instead of only generic verification progress.
- `statusboard.py` now surfaces the Proton handoff/manual-boundary state too, including the recommended handoff route and first checklist item, so even the quick `/status` view shows that signup likely finished and now needs deliberate recovery-material handling rather than more blind automation.
- Status aggregation got a responsiveness pass: `clawdy-brief.py` now runs independent subchecks in parallel and derives task-audit straight from `openclaw-status-summary.py` instead of re-running a second `openclaw status`, while `command-board.py` also fetches its sections in parallel. Result: the boards complete again in a single bounded pass instead of stacking unnecessary waits.
- Mail workflow gained a new `mail-focus.py` route plus `/mail-focus`, which picks the single best next mail to handle (unread first, otherwise latest) and attaches the matching draft suggestion when available; `mailboard.py` now surfaces that focus item directly.
- Mail heuristics are now shared in `mail_heuristics.py`, and the mail workflow gained deadline/tijdsdruk detection (for phrases like vandaag/morgen/EOD/voor 16:00): triage/focus/boards now surface a `deadline checken` action with a visible hint, and draft suggestions acknowledge the time pressure instead of treating those mails as generic replies.
- Proton manual-boundary observability was tightened: `automation-board.py`, `protonboard.py`, and `command-board.py` now include the `proton-manual-finish-summary.py` handoff when the flow reaches Recovery Kit/account-created state, including the top checklist item, so the first concrete web automation case is easier to finish without digging through lower-level scripts.
- Heartbeat cleanup: Proton handoff/status output no longer shows misleading `stale=True` once the flow is clearly at `account-created` / Recovery Kit manual-boundary; verified in `proton-manual-finish-summary.py` and `protonboard.py`.
- Mail workflow resilience got a real pass: `mailboard.py` now runs its latest/unread/summary/drafts/triage/focus subchecks in parallel with bounded timeouts and per-section graceful degradation, so one slow or failing mail subcommand no longer kills the whole board; verification confirmed `mailboard.py` and `command-board.py` still render correctly.
- Mail focus is now promoted into the top-level overviews too: `clawdy-brief.py`, `statusboard.py`, and `command-board.py` now surface the single best current mail plus whether a matching concept draft already exists, so the main status surfaces an immediately actionable next mail instead of only generic triage.
- Web automation observability now distinguishes better between operational managed sites and unmanaged/demo probes: `web-automation-sites.py` prioritizes configured stale sites first, exposes `operationally_healthy`, and both `automation-board.py` and `command-board.py` now show configured vs unmanaged stale counts so demo artifacts no longer obscure the real maintenance target.
- Mail workflow got a smarter anti-noise pass: repetitive verification-code mails are now tagged as `ephemeral_code`, get the `code gebruiken` action instead of generic reply treatment, no longer generate silly reply drafts, and `mail-focus.py` now returns no focus when the top window is only code-mail noise.
- Proton status reporting now suppresses stale/regression noise once the flow has clearly reached the Recovery Kit/manual boundary: `proton-status-summary.py`, `proton-next-step.py`, `automation-board.py`, and `command-board.py` treat `account_created` as the effective phase, stop suggesting a stale refresh there, and keep the handoff command prominent.
- Mail focus became more useful when the inbox tail is flooded by Proton verification-code noise: `mail-focus.py` now automatically looks further back in recent mail until it finds the first non-ephemeral actionable item, so `mailboard.py` and `command-board.py` can still surface a real human mail instead of returning no focus.
- Mail latest/brief views now have a meaningful-noise filter: `mail-latest.py` gained `--meaningful` plus deeper lookback, tags `ephemeral_code`/`no_reply`, and `mailboard.py` + `clawdy-brief.py` now use that route so top-level mail snapshots stop being dominated by Proton verification-code spam and surface the latest real human mail instead.
- Mail workflow got a compact thread view too: `mail-latest.py --threads` now groups recent mail by normalized subject/thread, collapsing noisy bursts into one conversation line; `mailboard.py` and `command-board.py` surface the hottest meaningful thread so the inbox snapshot shows the real conversation context instead of just the most recent individual message.
- Mail focus is now less misleading when there is no real inbox task left: `mail-focus.py` no longer falls back to an old read-only `ter info` message just because it is the newest non-code mail; it now returns no focus when the recent window is only Proton code noise plus stale informational mail, and `mailboard.py` reflects that cleanly.
- Mail recency ordering was corrected: `mail-latest.py` now parses and sorts by actual message date instead of assuming newest UID == newest mail, `mail-triage.py` uses that timestamp as a tiebreaker too, and verification showed the Bitwarden login-alert burst now appears in real chronological order in `mail-latest.py` and `mailboard.py`.
- Mail workflow fallback is now surfaced in the main boards too: when `mail-focus.py` finds no direct actionable mail because the recent window is dominated by Proton code noise, `mailboard.py`, `statusboard.py`, `clawdy-brief.py`, and `command-board.py` now show the best fallback thread plus how many code-mails were skipped, so the top-level views stay actionable instead of looking empty.
- Web automation observability now covers the desktop fallback layer too: new `desktop-fallback-status.py` plus `web-automation-dispatch.py desktop-status` report screenshot/window-capture freshness for `out-desktop*`, and both `automation-board.py` and `command-board.py` now surface that health directly, so the framework shows when the desktop rescue path itself has gone stale.
- Heartbeat follow-up: Proton signup is now documented more clearly as a manual-finish handoff instead of a generic automation blocker. Updated `scripts/proton-signup-notes.txt` with the current `account_created` / `recovery_kit` state plus the explicit checklist, and updated `STATUS.md` so the project board reflects that the next step is deliberate recovery-material handling and Pass verification.
- Mail heuristics now treat sender email as part of no-reply detection without polluting reply/question intent: `mail_heuristics.py` distinguishes content-vs-sender haystacks, tags Bitwarden-style `no-reply@...` alerts correctly, recognizes account-activation mails as `account activeren`, and stops generating silly reply drafts for no-reply verification/login alerts; verified via `mail-focus.py` and `mailboard.py`.
- Heartbeat polish: `proton-manual-finish-summary.py` now prioritizes the Recovery Kit save step ahead of login verification, and `statusboard.py` now reflects that safer first action (`sla de Recovery Kit bewust op ...`) in the top-level Proton handoff line.
- Mail triage/focus now treats suspicious login bursts more explicitly: `mail_heuristics.py` recognizes `new device logged in` / `unknown browser` as high-urgency `login-alert checken`, so `mail-triage.py`, `mail-focus.py`, and `statusboard.py` now surface Bitwarden-style login alerts ahead of generic account-activation mail.
- Heartbeat follow-up: `mail-focus.py` now counts same-subject/same-sender alert bursts across a wider triage window, and `mailboard.py` plus `statusboard.py` show that explicitly, so a Bitwarden login flood appears as one focus item with context like `(6x soortgelijk)` instead of feeling like six separate fresh tasks.
- Background build: mail-overviews now detect self-originated mailbox messages, hide their preview payloads, exclude them from `--meaningful` latest/fallback selection, and skip them as mail focus, so status boards no longer treat Clawdy's own test mail as the most meaningful inbox item or surface its body back in overview output.
- Heartbeat maintenance: checked `command-board.py`, confirmed Proton remains at the deliberate `account_created` / Recovery Kit manual boundary, and refreshed the only stale configured non-Proton site (`slack-signin`) via web-automation autopilot. Result: configured site health returned to healthy/operational; only unmanaged demo probes remain stale.
- Heartbeat polish: `clawdy-brief.py` and `statusboard.py` now also surface the hottest meaningful mail thread alongside the latest meaningful message, so the top-level overview keeps more conversation context instead of only one mail line.
- Mail noise filtering got another practical pass: obvious test messages (`testmail`, `this is a test`, etc.) are now excluded from meaningful-latest and mail-focus selection, so inbox overviews stop getting anchored on test traffic and fall through to the real Bitwarden security alert instead.
- Mail triage/drafts cleanup: recent triage now also drops self-messages and test mail entirely, and draft generation naturally stays empty for those cases, so the actionable mail pipeline is cleaner and no longer surfaces Clawdy's own mailbox traffic as a candidate item.
- Background build: mail security-focus now clusters related alert bursts instead of only exact-subject duplicates, so `mail-focus.py`, `mailboard.py`, `statusboard.py`, `clawdy-brief.py`, and `command-board.py` now report the Bitwarden login-alert wave as `7x verwant` rather than understating it as `6x soortgelijk` and missing the Firefox variant.
- Background build: `mail-latest.py --meaningful` now keeps important automated alerts instead of dropping every no-reply mail blindly, via shared `is_meaningful_message()` heuristics. Verified that Bitwarden login-alert mails now appear again in meaningful latest/thread views, `mailboard.py`, and `clawdy-brief.py`, while code-mail/self/test noise still stays filtered out.
- Heartbeat build: `web-automation-autopilot.py` now treats stale desktop-fallback artifacts as first-class maintenance after configured site refreshes, so one autopilot run can refresh Slack signin observability and then immediately renew `out-desktop` too; verified with a live run and `command-board.py`, which now reports `site health` as operational and `desktop fallback` as healthy/fresh.
- Heartbeat fix: `mailboard.py` now falls back to recent high-priority triage when unread is empty, so the board no longer claims `triage-high: 0` while `mail-focus` is clearly showing urgent Bitwarden login alerts; verified live with `mailboard.py`, now showing `triage-high: 4` and `hoge prioriteit (latest+high)`.
- Heartbeat build: `mail-triage.py` now annotates related-burst size per item and `mailboard.py` surfaces high-priority cluster counts, so a login-alert wave reads more honestly as one related burst instead of several unrelated urgent mails; verified live against the current Bitwarden alert flood.
- Background build: the web-automation site registry now supports generic `probeArgs`, so managed probe-page sites can carry sticky options like `--session slack-shared`; updated the registry/dispatch/docs, stored that session on `slack-signin`, and verified the configured sites board stays healthy with the new command rendering.
- Background build: mail tooling is now attachment-aware. `mail-latest.py` extracts attachment counts/names, heuristics now include attachment filenames in action detection, and the main mail/status boards show a compact 📎 hint for mails/threads with bijlagen; verified with live boards plus a synthetic invite/PDF example that resolves to `agenda checken`.
- Heartbeat polish: `mailboard.py` now distinguishes `hoog nieuw` from `hoog recent` when unread/new counts are zero but the recent window still contains urgent alerts, and `command-board.py` surfaces that too (`hoog recent 4`), so the inbox overview no longer understates live Bitwarden login-alert urgency.
- Heartbeat polish: `clawdy-brief.py` now also carries `hoog recent` mail urgency in its compact mailbox line, so the shortest status snapshot no longer hides that there are still 4 recent high-priority Bitwarden alerts even when unread/new stays at zero.
- Heartbeat polish: `statusboard.py` now mirrors that `hoog recent` mail hint too, so all main status surfaces stay consistent about lingering high-priority Bitwarden alert bursts even when the mailbox has no unread/new count left.
- Heartbeat build: `mail-latest.py --threads` now collapses related high-urgency no-reply alert variants per sender+action in a short time bucket, instead of splitting subject variants like Bitwarden `Unknown Browser` and `Firefox`; verified live via `mailboard.py` and `command-board.py`, which now show one 7x login-alert thread matching mail-focus.
- Heartbeat polish: grouped mail threads now also expose subject-variant count, so thread views/boards make it visible when one cluster includes multiple subject variants (for example Bitwarden login alerts now show `7x, +1 variant(en)` instead of looking like one exact-subject burst).
- Background build: `web-automation-artifacts.py` now distinguishes configured vs unmanaged stale artifacts and exposes `operationally_healthy`, with `automation-board.py` and `command-board.py` updated to surface that nuance so stale demo leftovers no longer look like a real managed-web-automation outage.
- Heartbeat build: mail heuristics now also recognize calendar-style attachment extensions (`.ics`, `.ical`, `.ifb`, `.vcs`) as `agenda checken`, so invite mails with vague subjects/previews but only a calendar file attachment no longer fall through as generic info/security noise.
- Heartbeat fix: desktop fallback summaries now expose a `display_*` reference for the freshest managed target when the global/default outdir is only an unmanaged stale demo artifact. `automation-board.py` and `command-board.py` now show that managed reference explicitly (currently `slack-signin`, fresh ≈3m) instead of implying the desktop layer itself is 3h+ stale.
- Follow-up heartbeat fix: `desktop-fallback-status.py` itself now also renders the probe URL/timestamp and screenshot from that same managed `display_*` target, so the detailed status view no longer mixes a fresh managed summary with stale unmanaged `example.com` probe details.
- Background build: mail tooling now extracts compact security-alert details from message previews (for example IP addresses from Bitwarden login alerts) and surfaces them in `mail-latest.py`, `mail-triage.py`, `mail-focus.py`, and `mailboard.py`, so clustered login-alert waves show concrete indicators instead of only generic high-priority subjects.
- Heartbeat fix: `mail-triage.py` now supports a deeper recent search window before applying `--high-only`/`--reply-only`, and `mailboard.py` plus `clawdy-brief.py` use that for recent high-priority summaries. Result: Bitwarden login-alert bursts no longer get undercounted in the boards; current mail focus and recent-high views now consistently show the same 7x related burst.
- Background build: mail overviews now show freshness too. Added reusable recency/span hints in `mail_heuristics.py`, threaded them through `mail-latest.py`, and surfaced them in `mail-triage.py`, `mail-focus.py`, `mailboard.py`, `statusboard.py`, `clawdy-brief.py`, and `command-board.py`, so clustered alerts now show when the latest message arrived and over what span the burst happened instead of only saying "7x".
- Heartbeat improvement: `mail-focus.py` now carries a burst-wide `security_alert_summary` onto the chosen focus item, so current Bitwarden-style login-alert focus lines now show the full related IP set (`84.87.67.160, 185.173.133.29`) instead of only the newest mail's single IP; verified live in `mail-focus.py` and `mailboard.py`.
- Follow-up heartbeat polish: top-level overviews now also surface those burst-wide security details more consistently. `command-board.py`, `clawdy-brief.py`, and `statusboard.py` render `format_security_alert_hint(...)` on mail thread/focus lines, so the Bitwarden alert cluster exposes the combined IP context higher up in the stack too; verified live in `command-board.py` and `clawdy-brief.py`.
- Heartbeat consistency pass: the same security hinting now also appears on `laatste betekenisvolle mail` lines, and in `clawdy-brief.py` also on triage/thread lines, so the mail overviews stay aligned instead of only the focus line carrying the useful Bitwarden IP context.
- Heartbeat maintenance: refreshed the desktop fallback probe via `python3 scripts/web-automation-dispatch.py desktop-probe` and verified with `desktop-status` / `automation-board.py` that desktop fallback is healthy again (`latest≈8s`, `screenshots=14`, `windows=True`).
- Background build: desktop fallback is now reusable per target instead of only one global outdir. `web-automation-dispatch.py desktop-probe` accepts `--slug <name>` plus optional `--url <url>`, `desktop-status` can inspect one named target via `--slug`/`--outdir`, and a live run verified `out-desktop-example-home` works cleanly via `desktop-probe --slug example-home --url https://example.com` followed by `desktop-status --slug example-home`.
- Background build: mail deadline heuristics now also understand explicit weekday/date deadlines like `uiterlijk vrijdag`, `Deadline 12/04`, and `by Tuesday 09:00` without falsely upgrading plain agenda mails like `meeting op maandag`; verified with synthetic samples plus fresh `mailboard.py` / `statusboard.py` runs.
- Background build: mailbox readers now share a retrying `mail_imap.py` helper for readonly IMAP access, so transient/concurrent login hiccups degrade less often. Updated `mail-latest.py`, `mail-verification-codes.py`, `proton-latest-code.py`, and `check_mail.py`; verified with `py_compile` plus parallel runs of `mailboard.py`, `mail-latest.py --meaningful --threads`, and `proton-latest-code.py --json`.
- Heartbeat fix: `mail-triage.py` now reports both shown vs total match counts before the `-n` cutoff, and `mailboard.py`, `statusboard.py`, plus `clawdy-brief.py` use those totals for recent high-priority summaries. Result: the mail overviews no longer underreport the current high-priority window as only 5 when the recent high set actually contains 18 items; verified live in all three boards plus `mail-triage.py --recent --high-only -n 5`.
- Background build: added `web-automation-prune.py` plus dispatch route `web-automation-dispatch.py prune-unmanaged`, a dry-run-first cleanup helper that inventories stale unmanaged probe artifacts and desktop fallback outdirs, and on `--apply` moves them to Trash via `gio trash` instead of deleting them. Updated `automation-board.py` and the browser automation README so boards now point at that cleanup path when only demo/debug leftovers are stale; verified with a live dry run showing the current `example-home` / `bitwarden-login` probe leftovers and unmanaged desktop outdirs.
- Background build: `web-automation-autopilot.py` now follows managed-site refreshes with a cleanup-review route instead of a vague `noop` when the stack is operationally healthy but stale unmanaged demo artifacts remain. Verified live: after refreshing Slack, autopilot now ends at `prune-unmanaged-review` with 6 current Trash candidates (4 site artifacts, 2 desktop outdirs).
- Background build: `automation-board.py` now gathers its catalog/Proton/artifact/site/desktop subchecks in parallel instead of sequentially, with the same graceful per-section degradation pattern as the other boards. Verified with `py_compile`, `automation-board.py --json` (`errors=[]`, `route_count=25`) and `command-board.py`, which still renders cleanly; measured runtime for `automation-board.py --json` is now about 0.7s in the current workspace.
- Heartbeat polish: mail overviews now show recent high-priority volume with cluster context too instead of only a raw count. Updated `mailboard.py`, `command-board.py`, `statusboard.py`, and `clawdy-brief.py` so lines like `hoog recent 18` now render as `hoog recent 18 in 3 cluster(s)` when relevant; verified live in all four boards against the current Bitwarden alert burst.
- Follow-up heartbeat polish: `statusboard.py` and `clawdy-brief.py` now also show subject-variant context on the hottest meaningful thread (`7x, +1 variant(en)`), so mixed alert bursts like the current Bitwarden Unknown Browser/Firefox wave are represented honestly in the compact top-level views too.
- Heartbeat build: `automation-board.py` now also runs the unmanaged-prune dry run and surfaces the actual cleanup candidate count, and `command-board.py` mirrors that (`prune-unmanaged (6 candidate(s))`), so the web automation overview now shows the concrete cleanup path instead of only vaguely saying unmanaged demo artifacts are stale.
- Background build: added `web-automation-refresh-desktop.py` plus dispatch route `refresh-desktop`, so stale configured desktop fallback targets can now be refreshed from observability just like managed site probes. Updated `web-automation-autopilot.py` to use that route first for configured stale desktop targets, and live verification refreshed `slack-signin` back to healthy so top-level boards now show desktop fallback operationally healthy with only unmanaged demo leftovers still stale.
