## 2026-04-10
- Late heartbeat build: `mail-next-step.py` geeft nu expliciet `mode=nu|review` plus `review_only`/`needs_attention_now` metadata, zodat stale-only follow-ups niet meer als impliciet urgente next action worden gepresenteerd. `mailboard.py`, `statusboard.py`, `command-board.py` en `clawdy-brief.py` labelen zulke gevallen nu ook eerlijk als `review`/`review-kandidaat` in plaats van generiek `mail next`. Live geverifieerd met tekst-runs van `mail-next-step.py`, `mailboard.py` en `statusboard.py`.
- Heartbeat consistency fix: `statusboard.py` en `clawdy-brief.py` gebruiken voor `hoog recent` nu expliciet de nieuwe high-only recency-totalen uit `mail-triage.py` (`total_high_attention_now_count` / `total_high_stale_attention_count`) in plaats van generieke attention-totalen. Daarmee blijft de samenvatting semantisch juist als die output later breder wordt hergebruikt. Live geverifieerd met `py_compile`, `statusboard.py` en `clawdy-brief.py`.
- Heartbeat polish: `mail-triage.py --clusters` geeft nu een duidelijkere header met high-priority recency-splitsing (`hoge mails X, actueel Y, niet actueel Z`) in plaats van het ambigue `hoog X`. Tijdens verificatie kwam ook een telling-bug boven water, waarna de output is gecorrigeerd naar echte high-only current/stale totalen. Live geverifieerd met `py_compile` en tekst/JSON-runs van `mail-dispatch.py clusters`.
- Heartbeat polish: `mail-triage.py --clusters` dubbelde de stale-marker per clusterregel (`[niet actueel] ... [niet actueel]`). Dat is nu gefixt door de render niet nog eens apart een stale-suffix te laten plakken bovenop `format_cluster_hint(...)`; live geverifieerd met `py_compile` en `mail-dispatch.py clusters -n 4`.
- Background build: `web-automation-autopilot.py --plan-only` geeft nu ook een compacte target-preview voor de huidige stap én de voorspelde follow-up (`target_summary`), zodat `automation-board.py` direct de echte queue kan tonen (`autopilot targets`, `autopilot daarna`) in plaats van alleen generieke aantallen. Live geverifieerd met tekst- en JSON-runs van beide scripts; huidige queue laat nu expliciet GitHub + Slack zien, daarna Bitwarden.
- Background build: `automation-board.py` en `command-board.py` tonen nu ook een compacte `stack queue` op basis van de volledige `stack-status` in plaats van alleen één `stack focus`. Daardoor blijft bijvoorbeeld een manual-boundary/handoff zoals `proton-signup [account-created]` zichtbaar als bredere observability-queue, ook wanneer autopilot zelf op `noop` staat. Live geverifieerd met `py_compile`, `automation-board.py` en `command-board.py`.
- Background build: `web-automation-stack-status.py` behandelt manual-boundary targets nu expliciet als handoff-ready in plaats van pseudo-stale automation-focus. `automation-board.py` en `command-board.py` tonen Proton daardoor nu als `manual-boundary, handoff-ready` met aparte observability-regel (`dom=...`) in plaats van misleidende `dom-stale` focus; live geverifieerd met `py_compile`, `web-automation-stack-status.py --configured-only --slug proton-signup`, `automation-board.py` en `command-board.py`.
- Heartbeat maintenance: de web-automation observability-stack stond weer op 3 stale beheerde niet-Proton targets (`github-login`, `slack-signin`, `bitwarden-login`) met tegelijk stale DOM en desktop fallback. Een `web-automation-dispatch.py autopilot --json` run ververste GitHub en Slack direct, daarna heeft `refresh-stack --json --configured-only --slug bitwarden-login` ook Bitwarden vernieuwd. Naverificatie met `automation-board.py` liet weer overal `stale=0`, `healthy=True`, `operational=True` zien voor artifacts, generic sites en desktop fallback.
- Background build: nieuwe `mail-next-step.py` kiest nu expliciet de volgende nuttige mailactie, ook wanneer `mail-focus` geen actuele unread-focus meer vindt. De helper prefereert een directe focus/draft, valt anders terug op actuele clusters en daarna op de beste recente high-priority follow-up (nu netjes de Bitwarden `login-alert checken` burst in plaats van Proton code-mail). `mail-dispatch.py` exposeert dit als `next-step` plus aliases `followup` en `next-mail-step`; live geverifieerd met tekst- en JSON-runs.
- Heartbeat build: `web-automation-artifacts.py` ondersteunt nu ook `--adapter ...`, zodat artifact-observability net als sites/desktop gericht op één adapter gefilterd kan worden. `web-automation-dispatch.py artifacts`, `browser-automation/README.md` en `TOOLING_INVENTORY.md` zijn bijgewerkt; live geverifieerd met `web-automation-artifacts.py --adapter github --json`, de dispatch-route en `py_compile`.
- Heartbeat maintenance: de actuele Bitwarden web-automation observability-stack is direct ververst via `python3 scripts/web-automation-dispatch.py refresh-stack --json --configured-only --slug bitwarden-login`. Resultaat: site `bitwarden-login` en desktop fallback vernieuwd, waarna `automation-board.py` en `command-board.py` overal weer `stale=0`, `healthy=True`, `operational=True` lieten zien.
- Heartbeat mail-consistency fix: `mail-next-step.py`, `mailboard.py`, `command-board.py`, `statusboard.py` en `clawdy-brief.py` tonen een gekozen stale follow-up nu ook expliciet als `[niet actueel]` in plaats van die context stil te verliezen. Gevalideerd met `py_compile`, `mail-next-step.py --json`, `mailboard.py --json` en tekst-runs van de hoofdboards op de huidige stale-only Bitwarden/Proton mailbox.
- Heartbeat build: `web-automation-autopilot.py` en `web-automation-prune.py` ondersteunen nu ook `--adapter ...`, zodat gerichte onderhoudsruns en cleanup-reviews veilig per adapter kunnen draaien in plaats van altijd de hele queue te raken. `web-automation-dispatch.py` en `browser-automation/README.md` zijn bijgewerkt; live geverifieerd met `autopilot --plan-only --adapter github`, `prune-unmanaged --adapter github` en `py_compile`.
- Heartbeat build: web automation registry ondersteunt nu `desktopKeepScreenshots`, en desktop status/refresh/site boards nemen die per-site retention automatisch mee (nu ingesteld op 6 voor GitHub en Slack).
- Heartbeat build: `mail-triage.py` kan nu ook directe clusterweergave geven via `--clusters`, zodat recente mailbursts compacter zichtbaar worden per afzender/actie in plaats van alleen als losse mails. `mail-dispatch.py` kreeg daarvoor een nieuwe `clusters` alias/route en `command-dispatch.py` exposeert nu ook `/mail-clusters`; live geverifieerd met alle drie de entrypoints op de huidige Proton/Bitwarden inbox.
- Heartbeat follow-up: `mail-focus.py` probeert fallback-threads nu eerst current-only (`mail-latest --meaningful --threads --current-only`) en valt pas daarna terug op stale betekenisvolle threads. Daardoor blijft de focus-route meteen actueel zodra er weer een echte lopende conversatie is, zonder oude fallback-noise te prefereren. Live geverifieerd met `mail-focus.py --json`, `mailboard.py` en `command-board.py` op de huidige stale-only inbox.
- Heartbeat build: beheerde web automation refresh-adviezen lopen nu via de hogere `refresh-sites --configured-only --slug ...` route, terwijl `web-automation-refresh-sites.py` intern gewoon het exacte low-level probe/route-commando blijft gebruiken. Daardoor wijzen `web-automation-sites.py`, `web-automation-artifacts.py`, `automation-board.py` en `command-board.py` nu naar één nette registry-aware onderhoudsroute in plaats van losse `probe-page` varianten.
- Live verificatie: `web-automation-refresh-sites.py --json --configured-only --slug slack-signin --max-sites 1` refreshte Slack succesvol via het interne sticky-session probe-commando, waarna `automation-board.py` nog maar 1 stale beheerde site overhield (Bitwarden).
- Heartbeat build: `web-automation-prune.py` groepeert prune-kandidaten nu ook per logisch target (`target_count` + `targets`), zodat unmanaged cleanup minder ruisig is dan alleen losse paden tellen.
- `automation-board.py` en `command-board.py` tonen die prune-review nu compacter en bruikbaarder als `6 path(s), 4 target(s) -> ...`, live geverifieerd met `prune-unmanaged` en `command-board.py`.
- Heartbeat polish: unmanaged desktop prune-targets krijgen nu een menselijker label op basis van slug of metadata-url (`example-home desktop`, `example.com desktop`) in plaats van alleen een rauw pad zoals `browser-automation/out-desktop`; live geverifieerd via `web-automation-prune.py --json` en `command-board.py`.
- Follow-up heartbeat polish: `automation-board.py` en `command-board.py` tonen prune-preview nu ook compacter als `label1, label2 +1 meer` wanneer er meer targets zijn dan de previewlimiet, zodat cleanup-hints niet stilzwijgend een derde target verbergen.
- Heartbeat maintenance: `web-automation-dispatch.py autopilot --json` pakte direct een echte stale beheerde site op en refreshte `bitwarden-login` succesvol; daarna bleef alleen de unmanaged example-demo cleanup-review over (`4` paths / `3` targets) en de beheerde web automation-stack was weer operationeel gezond.
- Mail workflow fix: `mail-triage.py` geeft nu ook totale `attention_now`/`stale_attention` tellingen terug, en `mailboard.py` gebruikt die voor `hoog recent`, zodat de samenvatting niet meer misleidend `niet actueel 5` toont terwijl er in werkelijkheid `18` recente high-priority mails in scope zijn; live geverifieerd met `mail-triage.py --json --high-only --all --search-limit 50` en `mailboard.py`.
- Heartbeat build: web automation kan nu per adapter gefilterd worden via `web-automation-sites.py --adapter ...` en `web-automation-refresh-sites.py --adapter ...`, plus dispatch-help/examples zijn bijgewerkt. Daarmee kunnen Slack/GitHub/Bitwarden adapters gerichter geobserveerd en ververst worden zonder board-ruis; live geverifieerd met `--adapter github` op beide scripts.
- Heartbeat polish: `desktop-fallback-status.py` gebruikt voor beheerde refresh-adviezen nu alleen de registry-aware slug-route (`python3 scripts/web-automation-dispatch.py desktop-probe --slug ...`) in plaats van ook nog een redundante `--url`, en `automation-board.py` toont die kortere refresh-lijn nu ook zo. Live geverifieerd via `desktop-fallback-status.py` en `automation-board.py`.
- Verificatie bracht ook een echte blocker boven water: `web-automation-refresh-desktop.py --max-targets 1` faalt momenteel voor `github-login` met `scrot: Can't open X display [:99]`, dus de desktop fallback refresh-route zelf is nu aantoonbaar stuk door ontbrekende X-display/context, niet door de observability-laag.
- Heartbeat fix: `browser-automation/desktop_probe.sh` wacht nu expliciet tot de X-display na `Xvfb` echt beschikbaar is en schrijft bij failures altijd `metadata.json` met een concrete fout in plaats van stil half stuk te vallen. Live geverifieerd met een verse `desktop-probe` voor `github-login`, die weer gezond artefacts/metadata opleverde (`startedXvfb=true`, `success=true`).
- Follow-up heartbeat fix: `web-automation-refresh-desktop.py` ondersteunt nu ook `--force`, zodat je een beheerd desktop target bewust kunt verversen ook als zijn eigen stale override nog niet verlopen is. Dispatch-help is bijgewerkt en live verificatie met `refresh-desktop --configured-only --slug github-login --force` gaf weer `candidate_count=1`, `success_count=1`.
- Heartbeat maintenance: `python3 scripts/web-automation-dispatch.py prune-unmanaged --apply` heeft 4 recoverable demo-artifacts/outdirs naar Trash verplaatst (`example-home` site probe plus `out-desktop*` demo-fallbacks). Daarna liet `command-board.py` zien dat unmanaged site- en desktop-stale counts op 0 staan en de web-automation stack weer schoner/duidelijker oogt.
- Heartbeat fix: `desktop-fallback-status.py` rekent `healthy` nu op basis van de actieve display-referentie (`display_outdir`) in plaats van alleen het legacy default-pad `browser-automation/out-desktop`. Daardoor tonen `command-board.py` en `automation-board.py` nu niet meer misleidend `healthy=False` wanneer alleen slug-based beheerde desktop targets bestaan; live geverifieerd met `desktop-fallback-status.py --json`, `command-board.py` en `automation-board.py`.
- Heartbeat cleanup/build: `web-automation-prune.py` neemt nu ook stale unmanaged losse automation-artifacts mee uit `web-automation-artifacts.py`, niet alleen site-probes en desktop-outdirs. Daarmee kon de oude `bitwarden-finish-signup` JSON/PNG veilig naar Trash, en `automation-board.py` toont nu artifacts `stale=0`, `healthy=True`, `operational=True`.
- Heartbeat build: `desktop-fallback-status.py` onderscheidt nu interne low-level desktop refresh-commando's van user-facing beheeradviezen. Daardoor tonen `automation-board.py` en `desktop-fallback-status.py` voor stale beheerde targets nu de nette `refresh-desktop --configured-only --slug ...` route in plaats van de rauwe `desktop-probe`, terwijl `web-automation-refresh-desktop.py` intern gewoon de probe-runner blijft gebruiken; live geverifieerd door `github-login` zo te verversen, waarna desktop fallback weer `stale=0`, `operational=True` werd.
- Heartbeat polish: `mailboard.py`, `clawdy-brief.py`, `statusboard.py` en `command-board.py` maken stale-only high-priority mail nu expliciet eerlijker zichtbaar. Als alle recente hoge clusters niet meer actueel zijn tonen ze nu `alles niet actueel`, en `triage eerst` krijgt ook een `[niet actueel]` hint zodat de statuslijnen minder urgent klinken dan de werkelijkheid. Live geverifieerd met de huidige Proton/Bitwarden mailset.
- Heartbeat onderhoud: `web-automation-dispatch.py autopilot --json` draaide nog twee veilige onderhoudsstappen. Eerst werden `slack-signin` en `github-login` opnieuw geprobed, daarna ook `bitwarden-login` plus de stale `slack-signin` desktop fallback. Resultaat: alle beheerde sites staan nu weer op `configured_stale_site_count=0` en desktop fallback is ook weer `configured_stale_outdir_count=0` / `operationally_healthy=true`.
- Heartbeat build: mail-thread/fallback output is nu recency-aware over de hele stack. `mail-latest.py` markeert threadgroepen als `attention_now` of `[niet actueel]`, en `mail-focus.py`, `mailboard.py`, `statusboard.py`, `clawdy-brief.py` plus `command-board.py` tonen daardoor eerlijke thread/fallback labels met leeftijd/span in plaats van oude Bitwarden-alerts als impliciet nog actief te presenteren; live geverifieerd met `mail-focus.py`, `mailboard.py` en `command-board.py`.
- Heartbeat maintenance: de twee actuele beheerde web-automation achterstanden zijn direct weggewerkt via `web-automation-refresh-sites.py --json --configured-only --slug bitwarden-login --max-sites 1` en `web-automation-refresh-desktop.py --json --configured-only --slug slack-signin --max-targets 1`. Naverificatie met `command-board.py` liet daarna overal weer `stale=0` zien voor artifacts, sites en desktop fallback, met de hele automation-stack terug op `operational=True`.
- Heartbeat fix: `command-board.py` en `automation-board.py` kapten site-adapter previews stilzwijgend af op 3 items, waardoor `slack:1` verdween zodra er 4 beheerde adapters waren. Previewlimiet staat nu op 4 met een expliciete `+N meer` suffix voor toekomstige groei; live geverifieerd met beide boards, die Slack nu weer netjes tonen.
- Follow-up heartbeat fix: `automation-board.py` kapte ook de `artifact adapters` preview nog op 3 af, waardoor Slack daar óók stil verdween terwijl `adapters=4` al werd gemeld. Die preview is nu eveneens 4 breed met dezelfde `+N meer` logica; live geverifieerd, nu met `bitwarden:1, github:1, proton:20, slack:1` zichtbaar.
- Heartbeat build: `web-automation-refresh-sites.py` en `web-automation-refresh-desktop.py` ondersteunen nu `--plan-only`, inclusief JSON- en tekstoutput met de exacte geplande commando's. `web-automation-dispatch.py` en `browser-automation/README.md` zijn bijgewerkt, zodat heartbeats/dashboards eerst veilig de refresh-queue kunnen inspecteren zonder meteen DOM- of desktop-artifacts te muteren; live geverifieerd met beide refresh-scripts plus de dispatch-routes.
- Heartbeat maintenance/build: `web-automation-dispatch.py autopilot --json` pakte de actuele GitHub-achterstand zelfstandig op, ververste eerst `github-login` site observability en daarna direct ook de stale desktop fallback. Naverificatie met `command-board.py` liet daarna overal weer `stale=0` / `operational=True` zien voor artifacts, sites en desktop fallback.
- Heartbeat maintenance: de actuele beheerde web-automation achterstand is weer weggewerkt via `web-automation-dispatch.py refresh-sites --configured-only --slug bitwarden-login`, `... refresh-sites --configured-only --slug slack-signin` en `... refresh-desktop --configured-only --slug slack-signin`. Naverificatie met `command-board.py` liet daarna opnieuw overal `configured stale=0` en `operational=True` zien voor artifacts, sites en desktop fallback.
- Heartbeat build: `web-automation-autopilot.py --plan-only` voert de gekozen stap nu ook echt via de onderliggende plan-only route uit en kan daarnaast de vermoedelijke vervolgactie previewen zodra de eerste stap zou slagen. `web-automation-dispatch.py` en `command-dispatch.py` beschrijven die autopilot-flow nu ook explicieter; live geverifieerd met `autopilot --json --plan-only`.
- Heartbeat polish: `mail-latest.py` ondersteunt nu `--current-only`, zodat snelle inbox/thread-views oude niet-actuele alerts direct kunnen wegfilteren in plaats van alleen te labelen. Daarbij krijgen losse mailregels nu ook een expliciete `[niet actueel]` marker, en `mail-dispatch.py` plus `TOOLING_INVENTORY.md` documenteren de nieuwe route. Live geverifieerd met `mail-latest.py --meaningful --current-only` (nu netjes `Geen mail gevonden` op de huidige stale-only inbox), `--threads`, `mail-dispatch.py catalog` en een regressierun van `mailboard.py`.
- Heartbeat follow-up: `mailboard.py` haalt nu ook aparte actuele betekenisvolle mail/thread-snapshots op via `mail-latest.py --current-only`, zodat het board vanzelf de actuele variant kan tonen zodra die bestaat, zonder de stale fallback-logica kwijt te raken. Op de huidige inbox blijft de output terecht bij `laatste betekenisvolle ...` omdat er nog niets actueels is; live geverifieerd met tekst- en JSON-runs van `mailboard.py`.
- Consistency pass: dezelfde current-aware mail/threadvoorkeur is nu ook doorgetrokken naar `clawdy-brief.py`, `statusboard.py` en `command-board.py`. Daardoor schakelen alle hogere overzichten automatisch naar `actuele betekenisvolle mail` / actieve thread zodra die bestaat, maar blijven ze op een eerlijke stale fallback staan als de inbox alleen oude alerts bevat. Live geverifieerd met alle drie de boards op de huidige stale-only mailbox.
- Kleine follow-up consistency fix: directe `mail focus`-regels in `clawdy-brief.py`, `statusboard.py` en `command-board.py` tonen nu ook een expliciete `[niet actueel]` marker als de focus-kandidaat stale is, net als triage/threadregels al deden. Op de huidige mailbox kwam de fallback-route in beeld, maar de drie boards renderden verder zonder regressie.
- Heartbeat bugfix: de echte oorzaak achter `status onbekend` in `clawdy-brief.py` / `statusboard.py` bleek niet de brief-timeout maar de interne 12s timeout in `openclaw-status-summary.py` zelf. Die staat nu op 30s, waarna `openclaw-status-summary.py --json`, `clawdy-brief.py` en `statusboard.py` weer gewoon live statusdata tonen in plaats van default `onbekend`-velden.
- Heartbeat build: desktop fallback probes ondersteunen nu screenshot-retentie via `--keep-screenshots`, en `refresh-desktop` kan die limiet ook doorgeven. Daardoor groeien langdurige `out-desktop-*` observability-targets niet eindeloos door, terwijl `desktop.png` plus een korte historie behouden blijft. Live geverifieerd met `github-login`: na `refresh-desktop --configured-only --slug github-login --force --keep-screenshots 2` toont `desktop-status` nog maar `3` screenshots totaal en `metadata.json` meldt `keepScreenshots=2`, `prunedScreenshotCount=2`.
- Heartbeat build: `mail-triage.py` ondersteunt nu ook `--current-only`, inclusief clusterweergave, zodat de workflow expliciet alleen nog actuele mails of bursts kan tonen in plaats van altijd een stale fallback mee te nemen. `mail-dispatch.py` documenteert dat, en `mailboard.py` gebruikt nu eerst current-aware triage voor de `triage nu`-regel en valt anders netjes terug zonder een pseudo-actuele kandidaat te suggereren. Live geverifieerd met `mail-triage.py --all --current-only` (nu correct 0 op de huidige stale-only inbox), `--clusters`, en `mailboard.py`.
- Heartbeat build: `web-automation-autopilot.py` ondersteunt nu ook `--max-actions`, zodat één run meerdere opeenvolgende onderhoudsstappen mag afhandelen zolang elke stap echte voortgang maakt. Dispatch-catalogi zijn bijgewerkt, syntax geverifieerd met `py_compile`, en daarna zijn de stale beheerde GitHub/Bitwarden/Slack site probes plus GitHub/Slack desktop fallbacks vernieuwd tot `command-board.py` overal weer `stale=0` / `operational=True` liet zien; `autopilot --plan-only --max-actions 4` eindigde daarna netjes op `noop`.
- Heartbeat build: desktop fallback is nu ook adapter-aware. `desktop-fallback-status.py` ondersteunt `--adapter ...`, `web-automation-refresh-desktop.py` kan desktop refresh-plannen en force-runs nu ook per adapter filteren, en `web-automation-dispatch.py` plus `browser-automation/README.md` documenteren dat. Live geverifieerd met `desktop-fallback-status.py --json --adapter github` en `web-automation-refresh-desktop.py --json --configured-only --adapter github --plan-only --force`.
- Heartbeat maintenance: de actuele stale beheerde site-probes voor `slack-signin` en `bitwarden-login` zijn meteen ververst via `web-automation-dispatch.py refresh-sites --json --configured-only --stale-after 900 --max-sites 2 --slug ...`. Naverificatie met `automation-board.py` zette artifacts en generic sites terug op `stale=0`, `healthy=True`, `operational=True`; alleen desktop fallback bleef nog stale maar niet meer blocking.
- Heartbeat build: web automation-overzichten zijn nu stack-aware. `web-automation-sites.py` berekent per beheerde slug ook een `refresh-stack`-advies, gebruikt dat automatisch als zowel DOM als desktop stale zijn, en `desktop-fallback-status.py` plus `automation-board.py` tonen nu ook die full-stack route naast losse site/desktop refreshes. Gevalideerd met `py_compile` en live JSON-output (`stack_command` zichtbaar voor `github-login`).
- Heartbeat build: `web-automation-autopilot.py` kiest nu ook direct `refresh-stack` zodra een beheerd niet-Proton target tegelijk stale DOM en stale desktop fallback heeft, in plaats van die twee onderhoudsstappen altijd los achter elkaar te plannen. Plan-only verificatie via `refresh-stack --json --plan-only --configured-only --slug slack-signin` en `web-automation-autopilot.py --json --plan-only` liep schoon.
- Heartbeat polish: `mailboard.py` maakt `triage-high` nu ook expliciet recency-aware wanneer het naar `latest+high` fallbackt, met dezelfde `actueel`/`niet actueel` en `alles niet actueel` hint als de `hoog recent` samenvatting. Live geverifieerd in tekst- en JSON-output op de huidige stale-only mailbox.
- Heartbeat consistency pass: `statusboard.py` en `clawdy-brief.py` tonen de `hoog recent` mailregel nu ook expliciet als `actueel X, niet actueel Y` in plaats van alleen `alles niet actueel` of alleen het actuele deel. Live geverifieerd op beide boards met de huidige stale-only mailbox.
- Heartbeat consistency pass: `command-board.py` toont diezelfde `hoog recent` recency-splitsing nu ook expliciet als `actueel X, niet actueel Y`, zodat alle hoofdboards weer dezelfde eerlijke mailsamenvatting geven. Live geverifieerd met `python3 scripts/command-board.py`.
- Background build: nieuwe drilldown-tool `scripts/mail-thread.py` laat nu één recente mailthread compact uitklappen, met filters op afzender/onderwerp/actie, huidige-vs-stale aandacht, berichttimeline, attachment/security hints en optioneel een bestaand concept. Live geverifieerd op de huidige Bitwarden alert-burst plus JSON-filtering via `--sender bitwarden`.
- Heartbeat build: `mail-thread` hangt nu ook echt aan de bestaande mail/command-dispatchers. `mail-dispatch.py` exposeert een nieuwe `thread` route met aliases, `command-dispatch.py` kent nu `/mail-thread`, en `TOOLING_INVENTORY.md` documenteert de capability. Live geverifieerd via `mail-dispatch.py catalog --json`, `/mail-thread --sender bitwarden --messages 2` en `py_compile`.
- Heartbeat follow-up: `mail-thread` kan nu ook exact op `--uid` selecteren, en `mail-next-step` gebruikt voor review-adviezen nu die concrete uid plus sender/action. Daardoor opent een stale/high follow-up zoals de Bitwarden `login-alert checken` burst veel betrouwbaarder de bedoelde thread in plaats van alleen de eerste globale sender/action-match. Live geverifieerd met de huidige Bitwarden-cluster (`uid 23`) en `py_compile`.
- Background build: `web-automation-autopilot.py --plan-only` voorspelt nu ook vervolgstappen nadat een `refresh-stack` maar een deel van de stale beheerde targets pakt. Daardoor tonen autopilot en `automation-board.py` nu netjes dat na GitHub+Slack nog `bitwarden-login` overblijft, in plaats van stil te stoppen zonder follow-up. Live geverifieerd met `py_compile`, `web-automation-autopilot.py --json --plan-only` en `automation-board.py --json`.
- Heartbeat build: `mail-next-step.py` checkt nu ook meteen of de gekozen current/stale follow-upthread al een bestaand concept heeft, en schakelt de adviesroute/command automatisch naar `mail thread --draft` wanneer dat zo is. Live geverifieerd met `py_compile` plus tekst- en JSON-runs van `mail-dispatch.py next-step`; op de huidige Bitwarden stale-followup staat nog geen concept klaar, dus de output bleef correct onveranderd zonder valse draft-hint.
- Heartbeat build: `command-dispatch.py` exposeert nu ook `/mail-next-step`, zodat de hogere slash-router dezelfde nuttige follow-uproute biedt als `mail-dispatch.py next-step`. Live geverifieerd met `py_compile`, een echte `/mail-next-step` run en `--json-help` waarin de nieuwe command zichtbaar is.
- Heartbeat polish: `mail-dispatch.py next-step` accepteert nu ook `--draft`, en `mail-next-step.py` toont dan bij tekstoutput meteen het concept als de gekozen stap er al één heeft. Catalogus/help zijn bijgewerkt en live geverifieerd met `py_compile`, `mail-dispatch.py next-step --draft` en `mail-dispatch.py catalog --json`; op de huidige Bitwarden stale-followup bleef de output terecht zonder drafttekst omdat er nog geen concept bestaat.
- Background build: `mail-latest.py` ondersteunt nu ook `--actionable`, zodat snelle inbox- of threadscans alleen concrete follow-upwaardige mails tonen (reply/deadline/security/alerts) zonder meteen alle overige noise mee te nemen. `mail-dispatch.py` catalogus/notitie zijn bijgewerkt en live geverifieerd met `mail-dispatch.py latest --actionable`, `--actionable --threads` en `mail-dispatch.py catalog`.
- Heartbeat build: `mailboard.py` haalt nu ook `mail-next-step.py` op en toont expliciet de aanbevolen vervolgstap plus exact review-commando wanneer er geen actuele focus meer is maar wel een zinvolle stale follow-up bestaat. `command-board.py` toont nu dezelfde compacte `mail next` samenvatting hogerop. Live geverifieerd met beide boards, die nu netjes de Bitwarden `login-alert checken` follow-up tonen.
- Heartbeat follow-up: dezelfde `mail next` hint zit nu ook in `clawdy-brief.py` en `statusboard.py`, zodat alle hoofd-overzichten consistent de aanbevolen stale follow-up tonen in plaats van alleen focus/triage. Live geverifieerd met beide scripts.
- Background build: nieuwe per-target stack-view `web-automation-stack-status.py` combineert nu DOM probe, desktop fallback, artifact-breadcrumbs en workflow-state in één gerichte observability-view voor adapterwerk. `web-automation-dispatch.py` exposeert dit als `stack-status` plus aliases `stack` en `site-stack`; `browser-automation/README.md` en `TOOLING_INVENTORY.md` zijn bijgewerkt. Live geverifieerd met `py_compile`, `stack-status --json` en `stack --slug github-login`.
- Background build: `automation-board.py` haalt nu automatisch een gerichte `stack_focus` drilldown op voor de eerste beheerde attention-target of expliciet gevraagde slug, en toont daardoor meteen `stack focus` plus `stack next` in het hoofdboard. `command-board.py` neemt diezelfde focus nu ook over. Tijdens verificatie kwam een echte latent bug boven water: `web-automation-stack-status.py --configured-only` stuurde die flag ten onrechte door naar `web-automation-artifacts.py`; dat is nu gefixt met gerichte filter-doorvoer. Live geverifieerd met `py_compile`, `automation-board.py --json`, `automation-board.py`, en `command-board.py`.
- Background build: `mail-next-step.py` kan nu ook een korte prioriteitenlijst van vervolgstappen geven via `-n/--limit`, inclusief commands, stale/draft-context en alternatieven. `mail-dispatch.py` exposeert dat nu ook als nieuwe `queue`/`worklist` route, `command-dispatch.py` als `/mail-queue`, en `TOOLING_INVENTORY.md` documenteert de capability. Live geverifieerd met `py_compile`, `mail-next-step.py -n 3`, `mail-dispatch.py queue` en `/mail-queue`.
- Heartbeat fix: de stale-indicator op `laatste betekenisvolle mail` ontbrak nog in meerdere hoofd-overzichten. Toegevoegd via gedeelde helper `format_stale_attention_hint(...)` in `mail_heuristics.py`, toegepast in `mailboard.py`, `statusboard.py`, `clawdy-brief.py` en `command-board.py`. Live geverifieerd: Bitwarden `gisteren 11:37` verschijnt nu consequent met `[niet actueel]` in de betrokken boards.
- Heartbeat maintenance: web-automation observability was weer stale op de drie beheerde niet-Proton stacks (`github-login`, `slack-signin`, `bitwarden-login`). Eerst draaide `web-automation-dispatch.py autopilot --json --max-actions 4`, waarmee GitHub en Slack werden ververst; daarna is de resterende Bitwarden-stack bewust afgewerkt via `refresh-stack --json --configured-only --slug bitwarden-login`. Naverificatie met `automation-board.py --json` gaf daarna overal `configured stale=0`, alle lagen `operationally_healthy=true`, en autopilot eindigde weer op `noop`.
- Heartbeat build: mail-overzichten tonen nu ook een compacte `mail queue` met alternatieve vervolgstappen uit `mail-next-step` in `mailboard.py`, `command-board.py`, `statusboard.py` en `clawdy-brief.py`. Daarbij vraagt `mailboard.py`/`clawdy-brief.py` nu bewust `mail-next-step -n 3` op, zodat de tweede kandidaat ook echt zichtbaar wordt. Live geverifieerd met alle vier de boards; huidige queue toont naast de stale Bitwarden login-alert follow-up nu ook `Verify Your Email` als alternatief.
- Heartbeat follow-up: dezelfde mail-overzichten tonen nu ook het concrete `mail review command` voor stale follow-ups, zodat de gebruiker vanuit een stale-only inbox direct de juiste thread-review kan openen zonder nog apart `mail-next-step` te hoeven draaien. Live geverifieerd in `mailboard.py`, `statusboard.py` en `clawdy-brief.py`; huidige Bitwarden follow-up toont nu expliciet `python3 scripts/mail-dispatch.py thread --uid 23 --sender no-reply@bitwarden.com --action 'login-alert checken' --messages 5`.
- Consistency follow-up: `mail-next-step.py` en `mail-dispatch.py next-step` tonen dat review-commando nu ook zelf bij stale follow-ups als `review-command=...`, zodat de losse tooluitvoer weer dezelfde directe affordance geeft als de hogere boards. Live geverifieerd op de huidige Bitwarden stale-followup.
- Background build: nieuwe `mail-review-next.py` opent nu direct de door `mail-next-step` gekozen vervolgmail als compacte thread-review, inclusief alternatieven en optioneel concept. `mail-dispatch.py` exposeert dit als `review-next` (ook aliases `review` en `next-thread`), `command-dispatch.py` als `/mail-review-next`, en `TOOLING_INVENTORY.md` is bijgewerkt. Live geverifieerd met `py_compile`, `mail-dispatch.py review-next` en `/mail-review-next` op de huidige Bitwarden stale-followup.
- Heartbeat fix: `mail-review-next.py` nummerde alternatieven per ongeluk vanaf `alt2`, terwijl `mail-next-step.py` al vanaf `alt1` telt. Dat is nu gelijkgetrokken, live geverifieerd met `mail-dispatch.py review-next`, dat nu netjes `alt1=...` toont voor de eerste alternatieve vervolgstap.
- Heartbeat follow-up: `mail-review-next.py` liet bij alternatieve vervolgstappen nog niet zien of daar al een concept voor klaarstaat, terwijl `mail-next-step.py` die context wel heeft. De review-output toont nu ook `[concept klaar]` op alternatieven wanneer relevant; live geverifieerd door `mail-dispatch.py review-next` naast `mail-dispatch.py queue` te draaien en te bevestigen dat de formats weer gelijk lopen.
- Heartbeat polish: `mail-review-next.py` labelt een stale review-route nu ook expliciet als `review-command=` in plaats van generiek `next-command=`, zodat de affordance overeenkomt met `mail-next-step.py` en duidelijker maakt dat het om een review-only follow-up gaat. Live geverifieerd met `mail-dispatch.py review-next` op de huidige Bitwarden stale-followup.
