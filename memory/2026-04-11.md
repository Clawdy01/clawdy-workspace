## 2026-04-11
- Heartbeat research: Exchange / Microsoft Graph route discovery kreeg een eerste concrete implementatienotitie in `research/exchange-graph-route-discovery.md`. Aanbevolen eerste route: delegated auth met smalle scope (`Calendars.Read` + `Tasks.Read`), agenda vooral via `GET /me/calendarView` voor een tijdvenster, en taken via Microsoft To Do routes (`/me/todo/lists`, `/me/todo/lists/{id}/tasks`) in plaats van legacy Outlook tasks. Dit geeft nu een duidelijke, kleine eerste implementatiestap voor kalender/taken zonder meteen mail of write-permissions erbij te trekken.
- Heartbeat build, track Exchange/Microsoft Graph: nieuwe helper `scripts/graph-setup-summary.py` plus `research/exchange-graph-setup-checklist.md` zetten de eerste proof-of-route nu om in een concrete setup-runbook. De helper valideert of `MSGRAPH_TENANT_ID`/`MSGRAPH_CLIENT_ID` al gezet zijn, toont aanbevolen read-only scopes, bouwt zodra mogelijk een authorize URL, en geeft direct de drie eerste Graph-routes voor agenda + To Do. Live geverifieerd met `py_compile`, tekstoutput en JSON-output; huidige blocker is nu scherp en klein: de tenant/client-id ontbreken nog.
- Vroege heartbeat mail-workflow fix: `mail-latest.py` ondersteunt nu echte `--action`- en `--urgency`-filters, zodat inbox-scans direct te beperken zijn tot bijvoorbeeld `security checken` of alleen `high`-mails. `mail-dispatch.py` catalogus/help is mee bijgewerkt; live geverifieerd met `mail-latest.py --help`, een JSON-run met beide filters en `mail-dispatch.py catalog`.
- Late heartbeat mail-workflow usability polish: `mail-next-step.py` en `mail-review-next.py` tonen alternatieve kandidaten nu ook met hun directe commando’s, en labelen die nu semantisch juist als `review-command=...` voor stale reviewkandidaten of `command=...` voor actuele routes. Live geverifieerd met `py_compile`; latere queue/review-runs vielen terug op `noop` omdat de mailbox intussen geen kandidaten meer teruggaf.
- Vroege heartbeat mail-workflow polish: `mailboard.py`, `statusboard.py`, `clawdy-brief.py` en `command-board.py` tonen bij `mail queue` nu ook een aparte `mail queue commands` regel voor de zichtbare alternatieven, zodat stale/current vervolgkandidaten vanaf de hoofdboards direct uitvoerbaar zijn zonder eerst nog `mail-review-next` of `mail-next-step` te openen. Live geverifieerd met de vier board-scripts; output toont nu o.a. `alt1 review-command=...` voor Bitwarden en `alt2 review-command=python3 scripts/mail-dispatch.py codes --sender Proton`.
- Vroege heartbeat Proton handoff polish: `proton-manual-finish-summary.py` geeft nu ook een direct `recommended_command`/`command` terug voor de bewuste manual boundary (`python3 scripts/web-automation-dispatch.py proton-manual-finish`). `command-board.py`, `statusboard.py`, `clawdy-brief.py` en `automation-board.py` tonen dat nu expliciet als `proton handoff command`, zodat de recovery-kit/account-created finishroute direct uitvoerbaar is vanaf de hoofd-overzichten. Live geverifieerd met `proton-manual-finish-summary.py`, `command-board.py`, `statusboard.py` en `clawdy-brief.py`.
- Vroege heartbeat Protonboard consistency polish: `protonboard.py` toont bij een manual boundary nu ook expliciet `handoff command: python3 scripts/web-automation-dispatch.py proton-manual-finish`, zodat ook het Proton-specifieke board dezelfde directe finishroute laat zien als de bredere status/automation-overzichten. Live geverifieerd met `python3 scripts/protonboard.py`.
- Late heartbeat build: threadreviews genereren draftsuggesties nu direct per gekozen thread via nieuwe gedeelde helper `mail_draft_helpers.py`, in plaats van alleen te vertrouwen op toevallige overlap met de losse top-N draftlijst. Daardoor kan `mail-thread --draft` / `mail-review-next --draft` ook voor een geselecteerde thread buiten de aparte draftscope nog een passend concept tonen. Live geverifieerd met `py_compile`, een echte Bitwarden threadrun en een synthetische threadtest die nu correct een reply-draft oplevert.
- Late heartbeat build: `mail-thread.py` haalt nu voor gekozen threads ook echte actielinks uit de ruwe mailbody/HTML, filtert ondersteunende/noise-links weg en geeft die door in zowel thread- als `mail-review-next` output. Daardoor tonen Bitwarden `Verify Your Email` en `New Device Logged In...` reviews nu direct de bruikbare verificatie-/vault-links in plaats van alleen een afgekapt preview-fragment. Live geverifieerd met `py_compile`, `mail-dispatch.py thread --uid 17 ...`, `mail-dispatch.py review-next --candidate 2` en `--candidate 1`.
- Late heartbeat mail-workflow consistency polish: `mail-next-step.py` rendert geselecteerde en alternatieve kandidaten nu via gedeelde helper `format_next_step_candidate_hint(...)`, waardoor queue/next-step tekstoutput dezelfde rijke hints toont als de boards en `mail-review-next` (count/security/age/stale/draft) in plaats van een eenvoudiger eigen formatter. Live geverifieerd met `py_compile`, `mail-dispatch.py queue` en `mail-dispatch.py next-step -n 3`.
- Late heartbeat mail-review polish: `mail-review-next.py` hergebruikt nu `format_next_step_candidate_hint(...)`, waardoor alternatieven dezelfde rijke samenvatting tonen als de hoofdboards (aantal/security/age/stale). Voor `check-codes` reviews toont de tekstoutput nu ook expliciet de gekozen code-broncluster. Live geverifieerd met `py_compile`, `mail-dispatch.py review-next` en `mail-dispatch.py review-next --candidate 3`.
- Heartbeat mail-workflow polish: `mail-thread.py` verrijkt threadreviews nu met geaggregeerde security-alert context (`security_alert_summary`), subject-varianten, `latest_age_hint` en `span_hint`, zodat één review direct laat zien welke IP-burst/varianten in een security-thread zitten. Live geverifieerd met `py_compile` en een echte Bitwarden-threadrun; output toont nu o.a. `+1 variant(en)`, `28m span` en `{IP 84.87.67.160, 185.173.133.29}` in de header.
- Heartbeat mail-workflow fix: `mail-thread.py` geeft nu ook zijn eigen user-facing `command` terug in JSON, zodat `mail-review-next.py` niet langer stil `thread_command=null` houdt wanneer het een concrete review opent. Live geverifieerd met `mail-dispatch.py thread --json` en `mail-dispatch.py review-next --json`.
- Background build: `mail-review-next.py` kan nu ook alternatieve queue-items direct openen via `--candidate N` in plaats van alleen de topaanbeveling. `mail-dispatch.py` catalogus/help en `TOOLING_INVENTORY.md` zijn bijgewerkt; live geverifieerd met `py_compile`, `mail-dispatch.py review-next --candidate 2`, `--json` en `catalog --json`.
- Heartbeat mail-workflow polish: `mail-review-next.py` neemt nu ook de rijkere threadheader over uit `mail-thread.py`, inclusief subject-varianten en tijdspan (`latest_age_hint` / `span_hint`). Live geverifieerd met `mail-dispatch.py review-next` en `--candidate 2`; output toont nu bv. `7x, +1 variant(en), 2026-04-09 11:37, 28m span`.
- Heartbeat mail-workflow fix: `mail-review-next.py` toont in tekstoutput nu het echte user-facing review-commando van de gekozen kandidaat, in plaats van soms een intern `mail-thread --draft` lookup-commando te lekken terwijl er geen concept klaarstaat. Live geverifieerd met `mail-dispatch.py review-next` en `--candidate 2`; beide tonen nu weer nette `mail-dispatch.py thread ... --messages 5` commands zonder onnodige `--draft`.
- Late heartbeat fix: `mail-review-next.py` nummerde alternatieven in tekstoutput weer met hun oorspronkelijke kandidaatindex, waardoor bij `candidate=1/2` de enige fallback als `alt2` verscheen. De renderer gebruikt nu weer een compacte doorlopende alt-telling voor alleen de getoonde alternatieven. Live geverifieerd met `py_compile` en `mail-dispatch.py review-next`, dat nu correct `alt1=Bitwarden — Verify Your Email ...` toont.
- Late heartbeat consistency fix: `mail-review-next.py --json` gaf bij gekozen alternatieve kandidaten soms nog een intern lookup-commando met onnodige `--draft` terug onder `thread_command`, ook wanneer er geen concept bestaat. De payload gebruikt nu standaard het echte user-facing `recommended_command` van de gekozen kandidaat. Live geverifieerd met `py_compile` en `mail-dispatch.py review-next --candidate 2 --json`, dat nu een schoon `mail-dispatch.py thread ... --messages 5` commando teruggeeft.
- Late heartbeat maintenance: web-automation observability stond weer stale op GitHub, Slack en Bitwarden. `web-automation-dispatch.py autopilot --json --max-actions 4` werkte GitHub en Slack zelfstandig bij; daarna is de resterende Bitwarden-stack expliciet afgewerkt via `refresh-stack --json --configured-only --slug bitwarden-login`. Naverificatie met `automation-board.py` en `command-board.py` liet daarna overal weer `stale=0`, `healthy=True`, `operational=True` zien voor artifacts, sites en desktop fallback; alleen Proton blijft terecht als manual-boundary handoff over.
- Late heartbeat build: `command-board.py` toont nu ook de web-automation autopilot-samenvatting (`autopilot next/targets/plan/daarna`) en rekent desktop fallback eerlijker door met `blocking` targets plus effectieve `operational` status, net als `automation-board.py`. Live geverifieerd met `py_compile` en `python3 scripts/command-board.py`; het hoofdboard laat nu direct de beste multi-target onderhoudsrun en vervolgstap zien.
- Late heartbeat mail-workflow follow-up: `mail-next-step.py` voegt nu ook de beste verificatiecode-cluster als aparte kandidaat toe, zelfs wanneer gewone review-followups hoger staan. `mail-review-next.py` behandelt `check-codes` nu route-specifiek in plaats van zo'n kandidaat per ongeluk als threadreview te openen. Live geverifieerd met `py_compile`, `mail-dispatch.py queue` (nu met `alt2=Proton — Proton Verification Code`) en `mail-dispatch.py review-next --candidate 3`, dat nu correct een codes-overzicht toont met `review-command=python3 scripts/mail-dispatch.py codes --sender Proton`.
- Late heartbeat mail-workflow polish: `mail-verification-codes.py` filtert nu footer/URL-ruis beter weg bij verificatiecode-detectie. Proton-codes gaven eerder vals ook `2024` en `1228` terug; de extractor gebruikt nu primair 6-cijfercodes plus contextuele fallback voor 4-8 cijfers, met simpele noise-filters voor jaartallen/tijdachtige resten. Live geverifieerd met `py_compile`, `mail-dispatch.py codes --sender Proton` en `mail-dispatch.py review-next --candidate 3`, die nu alleen de echte codes tonen.
- Late heartbeat web-automation polish: `command-dispatch.py` exposeert nu ook de nieuwe stackgerichte routes `/automation-stack`, `/automation-refresh-desktop` en `/automation-refresh-stack`, zodat de gecombineerde DOM/desktop observability-tools net zo makkelijk oproepbaar zijn als de oudere site-routes. Live geverifieerd met `/automation-stack` en `/help`, die de nieuwe commands nu correct tonen.
- Background build: `mail-next-step.py` doet draft/thread lookups voor groepskandidaten nu pas ná ranking en alleen voor de getoonde topkandidaten, in plaats van eager voor elke current/high kandidaat. Daardoor blijft queue/review-output hetzelfde maar met minder onnodige IMAP/threadwerk; live geverifieerd met `py_compile`, `mail-dispatch.py queue`, `mail-dispatch.py review-next` en timingruns (~16.3s -> ~15.6s voor `queue`).
- New collaboration rule from Christian: if Clawdy has been building for more than about 3 hours without solving real tasks / producing real results, hard-stop that track and consult on what is actually useful. In the meantime, pick the next real task, also with a hard 3-hour stop if there is no result.
- Vroege heartbeat maintenance: web-automation observability stond opnieuw stale op Bitwarden, Slack en GitHub. `web-automation-dispatch.py autopilot --json --max-actions 4` werkte Bitwarden en Slack bij; daarna heeft `refresh-stack --json --configured-only --slug github-login` de resterende GitHub-stack afgerond. Naverificatie met `autopilot --plan-only` liet daarna overal `configured_stale_site_count=0`, `configured_stale_outdir_count=0`, `healthy=True`, `operationally_healthy=True` zien; alleen Proton blijft terecht als manual-boundary handoff.
- Heartbeat build, track Exchange/Microsoft Graph: `graph-setup-summary.py` geeft nu behalve scopes/routes ook directe `export ...` env-regels, een scope-string en concrete `curl` voorbeelden voor token exchange, `calendarView` en To Do. De setup-checklist is daarop bijgewerkt. Live geverifieerd met `py_compile`, tekstoutput en JSON-output; blocker blijft klein en scherp: `MSGRAPH_TENANT_ID` en `MSGRAPH_CLIENT_ID` ontbreken nog, dus authorize/token-URLs kunnen pas daarna volledig worden opgebouwd.
- Christian wants reactions to kids and family-related messages treated with priority at all times; these should never be treated as background tasks.
- New collaboration rule from Christian: when a useful lesson or correction is learned, Clawdy should apply it immediately to the messages/actions that directly preceded it, not only to future cases.
- Christian wants Clawdy to always use the right LLM/model/tooling for the task, rather than taking a simplistic handmade or weaker route when a better model is appropriate.
- New collaboration rule from Christian: when Clawdy promises or starts a concrete deliverable, it must keep that open loop visible and not let later messages or heartbeats displace it without either delivering it or explicitly stating the blocker. Responding to the latest message is not enough if the previous concrete request is still unresolved.
- New collaboration rule from Christian: after a clear instruction or approval, Clawdy should not ask for the same approval again. Treat a clear instruction as an execution commit, not as something to reconfirm. Re-asking for permission after clarity is a recurring frustration and should be structurally avoided.
- New collaboration rule from Christian: if a requested deliverable depends on a tool/model that is not actually available, Clawdy must stop immediately and report the blocker plainly instead of drifting into capability discovery, meta-analysis, or placeholder work.
- Heartbeat research: `research/memory-techniques-recommendation.md` toegevoegd met concrete aanbeveling voor de huidige OpenClaw-memoryopzet. Advies: file-based 3-lagenmodel behouden (daily episodic, curated MEMORY, procedureel via AGENTS/skills), `memory_search` selectief blijven gebruiken, geen vector DB nu, en vooral structureren/consolideren met vaste daily labels zoals `Besluit`, `Les`, `Blocker`, `Voorkeur`, `Deliverable`.
- Heartbeat build, track Exchange/Microsoft Graph: nieuwe helper `scripts/graph-proof.py` toegevoegd voor de eerste echte read-only proof-of-route zodra tenant/client-id er zijn. De helper accepteert een auth code of volledige redirect URL, doet de token exchange lokaal, en test daarna direct `GET /me/calendarView`, `GET /me/todo/lists` en optioneel `GET /me/todo/lists/{id}/tasks`. Checklist in `research/exchange-graph-setup-checklist.md` bijgewerkt; live geverifieerd met `py_compile` en `--help`.
