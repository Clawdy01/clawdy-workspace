## 2026-04-08
- Christian wants Clawdy to move beyond preparation and get up to speed quickly with real capability gains.
- OpenClaw ecosystem notes were clarified: relevant external sources are mainly skills and plugins, with a preference to adopt skills freely and only install plugins after code/risk review.
- Browser/web automation was advanced into a reusable framework with dispatch routes spanning DOM/Playwright and desktop fallback, not just one-off Proton hacks.
- Proton signup automation now reliably reaches password-step and submit-ready with a strong generated password, avoiding false weak-password noise.
- A dedicated final Proton submit probe was added, plus controlled real submit attempts to observe the true blocker.
- The true Proton signup blocker is now confirmed as Human Verification via email with a “Get verification code” step, not a generic captcha guess.
- Mail-side helpers were built for verification handling: `mail-verification-codes.py` extracts likely verification codes from recent mail and `proton-verification-status.py` combines Proton flow state with mailbox code lookup.
- `proton-verification-status.py` evolved into a small workflow/state machine: supports refresh, refresh-submit, auto progression, stale-state detection, recommended next action, and caching.
- The current stable Proton state can be driven in one step back to email verification, after which the flow waits for a real verification email/code.
- Status/observability tooling was repeatedly hardened: `statusboard.py` and `clawdy-brief.py` gained more graceful degradation and later Proton verification integration.
- A recurring performance lesson surfaced: expensive status subchecks should use caching/degradation so the main statusboard remains responsive.
- Proton verification status is now visible in normal status output, including fields like screen/email/matches and later stale/next guidance.
- Current practical Proton blocker at flush time: still no matching Proton verification email/code has arrived, so signup cannot finish yet.
- Mail workflow gained a new `mail-triage.py` route plus dispatcher commands (`mail-dispatch triage`, `/mail-triage`) that prioritize unread mail with action hints and reply-needed signals, and `mailboard.py` now surfaces the top triage item.
- Proton signup automation now has a dedicated `proton_human_verification.js` step that safely reaches the Human Verification modal, inspects the actual visible controls, and can later drive email/code entry when explicitly invoked with flags.
- The current Human Verification UI is now concretely mapped: one visible `Email address` field plus a `Get verification code` button, exposed through `proton-human-verification`, `/proton-human`, and a compact summary script.
- `clawdy-brief.py` and `statusboard.py` now surface the top mail triage item directly, using recent mail when there is no unread mail, so the main overview shows not just the latest message but also the recommended first mail action.
- `proton-verification-status.py` now folds in the dedicated human-verification probe output, so Proton status stays truthful even when the last submit-probe itself did not show the modal; current combined recommendation resolves to `wait-for-mail` instead of the misleading `refresh-submit`.
- `command-board.py` now uses the richer `automation-board.py` data instead of only the automation catalog, so the main board shows route count plus artifact health, generic site health, and the current recommended Proton next step in one place.
- `command-dispatch.py` now exposes `/automation-artifacts` and `/proton-request-code`, so the richer web automation observability and the next Proton verification step are directly reachable from the normal command surface instead of only through lower-level scripts.
- `proton-refresh-safe.py` was hardened so it now captures per-step stdout/stderr, records which safe Proton step failed or timed out, and still attempts to produce a status summary instead of only dying with one opaque Playwright error.
- `proton-next-step.py` now explicitly recognizes the current Proton regression pattern where username propagation still works but the password step no longer appears, and surfaces that as `investigate-password-regression` instead of pretending this is just a normal next step.
- `automation-board.py` and `command-board.py` now surface Proton regression suspicion and verification staleness directly in the board text, so the high-level overview shows both the likely selector/flow break and whether verification evidence itself is stale.
- `proton_human_verification.js` now fails gracefully with structured output when the password step never appears, capturing a snapshot that proves username propagation still worked while the flow stayed on the start page; this turns the current Proton issue into a clean diagnosable artifact instead of a bare Playwright timeout.
- Proton verification tracking now incorporates the latest external code-finish artifact, including the used verification code and whether the flow appears to have returned to password setup; `proton-next-step.py` uses that to recommend `continue-password-setup` instead of treating the state as generic regression/no-progress.
- `automation-board.py` and `command-board.py` now surface the used Proton verification code, whether password setup looks ready again, and the refined next action, so the high-level status finally matches the more advanced Proton verification state instead of hiding that progress.
- `protonboard.py` now also shows the richer verification state, including used code, whether password setup looks ready again, staleness, and the refined next action, so the dedicated Proton overview no longer lags behind the newer verification pipeline.
- `protonboard.py` and `automation-board.py` now degrade gracefully with bounded subprocess timeouts instead of dying on a single slow child command, which keeps the higher-level web automation overviews usable even when one underlying probe hangs.
- `web-automation-autopilot.py` no longer passes a bogus `--timeout` flag to `refresh-sites`; instead it keeps timeout handling internal and forwards a clean command. That removes one concrete failure mode from the autonomous web-maintenance path.
- The web automation autopilot now also applies an explicit bounded timeout to the `proton-autopilot-safe` path, so autonomous maintenance is less likely to hang indefinitely in long Proton flows and get killed externally without a useful result.
- Web automation observability became more reusable: `web-automation-artifacts.py` now scans `browser-automation/out*`, groups artifacts per adapter, reports freshness/screenshot paths, and is exposed via `web-automation-dispatch.py artifacts`; `automation-board.py` now shows adapter/artifact freshness directly.
- Mail drafts were extended beyond only "new since state": `mail-drafts.py` now supports unread and recent scopes via `--unread` / `--all`, and `mailboard.py` now uses unread-driven draft suggestions so reply-ready mail can surface even when it is no longer "new".
- Generic web automation probes are now promoted into reusable site-level observability via `web-automation-sites.py` and the new `web-automation-dispatch.py sites` route, so per-site slugs/URLs/titles/forms/refresh commands are visible without digging through raw probe JSON.
- Proton signup now has an explicit `proton-use-verification-code.py` helper plus `proton-use-code` dispatcher route, which pulls the latest Proton code from mail and can drive the Human Verification step once the verification email arrives.
- Generic web automation observability can now self-refresh: `web-automation-refresh-sites.py` plus dispatcher routes `refresh-sites`, `/automation-sites`, and `/automation-refresh-sites` re-run saved site probes from stored slug/url metadata, and the stale generic site artifacts were refreshed back to healthy.
- Web automation gained a safe `autopilot` workflow via `web-automation-autopilot.py`, exposed through `web-automation-dispatch.py autopilot` and `/automation-auto`; it now refreshes stale non-Proton site probes first and only then considers safe Proton next steps, avoiding accidental expensive Proton refresh loops when the real next issue is a Proton password-step regression.
- `web-automation-autopilot.py` now chains work more usefully: after a successful generic site refresh it re-evaluates state and can continue directly into one safe Proton step in the same run, so background automation can both keep observability fresh and advance the real signup workflow instead of stopping halfway.
- Proton verification state handling now also reads the request-code artifact, recognizes when code-entry is already active, and promotes existing mailbox codes into the next-step/autopilot decision instead of falling back to a stale password-regression loop.
- The next real Proton blocker is now sharper: `proton-use-code` tries the right route, but `proton_human_verification.js` still rebuilds from `#password` and times out there under the current password-step regression, so code-use needs a resume-from-verification path instead of replaying the broken earlier transition.
- `proton-use-verification-code.py` now uses the stable external-email finish route instead of the broken `#password` rebuild path, and a real verification run succeeded: it fetched a fresh mailbox code, filled the verification field, and reached a state where the final `Get started` button was visible and enabled.
- Proton verification/status reporting now also recognizes the deeper `proton-finish-signup-with-code` Recovery Kit artifact as an account-created state, so `proton-verification-status.py`, `proton-next-step.py`, and the automation/command boards no longer keep suggesting another password/code loop after the signup flow already crossed into Recovery Kit handling.
- Proton verification state handling was tightened to prefer fresher evidence over older Recovery Kit artifacts: if a newer external-finish artifact shows the flow back at password setup, `proton-verification-status.py` now treats that as the active state, exposes `account_created_seen` separately, and `proton-next-step.py` switches back to `continue-password-setup` instead of falsely declaring the account already finished.
- `continue-password-setup` is now a first-class Proton route instead of a misleading alias to `proton-use-code`: new wrapper `proton-continue-password-setup.py` uses the external-email finish path directly, `proton-next-step.py` and both dispatchers point to it, and `proton-autopilot-safe.py` now gives that heavier step a longer timeout plus graceful timeout handling instead of crashing.
- A live verification run of the new continue route succeeded with a fresh code and updated the Proton artifacts again; afterwards the next-step state flipped back to `account-created`, which means the Recovery Kit / account-created path is once again the freshest visible state.
- Christian prefers background progress updates to be concise rather than full text, and reduced to roughly every 2 hours to keep the chat overzichtelijk.
- Communication reliability issue was repaired by switching key cron updates to explicit direct Telegram delivery, which Christian confirmed is now working well.
